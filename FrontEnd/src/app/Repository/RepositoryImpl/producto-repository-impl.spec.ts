import { ProductoRepositoryImpl } from './producto-repository-impl';

describe('ProductoRepositoryImpl', () => {
  it('should create an instance', () => {
    expect(new ProductoRepositoryImpl()).toBeTruthy();
  });
});
